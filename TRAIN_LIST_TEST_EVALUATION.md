# 新增车次列表测试用例评估报告

## 1. 接口文件评估 (`.artifacts/api_interface.yml`)

针对车次列表相关的核心 API 端点进行评估：

| API 端点 ID | 描述 | 覆盖情况 | 缺失场景 |
| :--- | :--- | :--- | :--- |
| **API-GET-Trains** | 获取车次列表 | ✅ **部分覆盖** | - 排序参数 (`sortBy`, `sortOrder`)<br>- 时间范围筛选 (`departureTimeStart/End`)<br>- 鉴权失败 (401)<br>- 席别筛选 (`seatTypes`) |
| **API-GET-Trains-Search** | 精确/范围搜索 | ✅ **部分覆盖** | - 合法的时间范围搜索<br>- 组合查询 (车次号 + 时间)<br>- 鉴权失败 (401) |
| **API-GET-Train-Schedule** | 获取时刻表 | ✅ **基本覆盖** | - 鉴权失败 (401)<br>- 服务端错误 (500) |
| **API-GET-Trains-RoundTrip** | 双程查询 | ✅ **基本覆盖** | - 排序与筛选参数<br>- 鉴权失败 (401) |
| **API-GET-Stations-Groups** | 站点分组 | ❌ **未覆盖** | - 缺少对应的后端路由测试文件 |

**评估总结：**
*   **参数组合：** 核心必填参数（出发/到达/日期）覆盖较好，但**可选参数的组合**（如“学生票+排序+时间筛选”）覆盖率低。
*   **异常场景：** 覆盖了基本的参数缺失 (400) 和资源不存在 (404)，但缺少**权限验证 (401)** 和**系统错误 (500)** 的测试。

## 2. 需求文档评估 (`requirements/04_车次列表页/04_车次列表页.md`)

针对需求文档中的功能点进行覆盖度分析：

| 需求模块 | 功能点 | 覆盖状态 | 备注 |
| :--- | :--- | :--- | :--- |
| **3.1.3 车次查询栏** | 站点/日期选择 | ✅ **覆盖** | 包含单程/双程、普通/学生切换 |
| | 站点下拉分组 (热门/ABCDE) | ✅ **覆盖** | 前端 `StationGroupSelector` 已测试 |
| | **默认状态/输入校验** | ❌ **缺失** | 未测试“出行信息不全点击查询无反应” |
| **3.1.4 详细筛选** | 筛选条件 (车次类型/时间等) | ⚠️ **部分覆盖** | 缺少“选择后立即更新（无需点击查询）”的交互测试 |
| | 14天日期栏 | ❌ **缺失** | 未测试日期栏的渲染与切换 |
| **3.1.5 信息展示** | 列车基础信息 (车次/时间/历时) | ✅ **覆盖** | `TrainList` 测试已包含 |
| | **席别状态逻辑** | ✅ **覆盖** | 充分覆盖了“候补/有/数字/—”四种状态 |
| **3.2 页面跳转** | 预订按钮跳转 | ✅ **覆盖** | 已测试未登录跳登录页 |
| | **顶部导航跳转** | ❌ **缺失** | 未测试“我的12306”、“退出”等导航逻辑 |

## 3. 覆盖率报告

*   **接口端点覆盖率：** **83%** (5/6，缺失 `API-GET-Stations-Groups` 后端测试)
*   **需求点覆盖率：** **约 70%** (核心展示与查询逻辑已覆盖，交互细节与边缘规则缺失)
*   **参数组合覆盖率：** **< 50%** (主要集中在 Happy Path，缺乏复杂组合测试)

## 4. 改进建议

**优先级：高 (High)**
1.  **补充后端测试：** 创建 `backend/test/routes/stationsGroups.test.js`，确保站点分组接口可用。
2.  **增强 API 测试：** 在 `trains.test.js` 中增加针对 `sortBy` (排序)、`departureTime` (时间段)、`seatTypes` (席别) 的测试用例。
3.  **补充鉴权测试：** 为所有受保护的 API 添加 `401 Unauthorized` 测试用例。

**优先级：中 (Medium)**
1.  **完善前端交互测试：** 在 `TrainList.test.tsx` 或集成测试中，验证“筛选条件改变时立即触发列表更新”的需求。
2.  **增加边界测试：** 测试“点击查询但参数不全”的场景（预期无网络请求）。
3.  **补充导航测试：** 增加顶部导航栏的登录/退出/个人中心跳转测试。

**预估工作量：**
*   后端测试补充：0.5 人天
*   前端交互测试补充：0.5 人天
*   **总计：1 人天**
