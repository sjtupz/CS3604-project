// TODO: 实现用户相关API调用
// import apiClient from './client';

// 用户信息接口
// export const getUserInfo = async () => {
//   const response = await apiClient.get('/api/user/info');
//   return response.data;
// };

// 更新用户联系方式
// export const updateUserContact = async (contactData: {
//   phoneNumber: string;
//   email: string;
// }) => {
//   const response = await apiClient.put('/api/user/contact', contactData);
//   return response.data;
// };

// 更新用户优惠类型
// export const updateUserDiscountType = async (discountData: {
//   discountType: string;
//   studentQualification?: object;
// }) => {
//   const response = await apiClient.put('/api/user/discount-type', discountData);
//   return response.data;
// };
